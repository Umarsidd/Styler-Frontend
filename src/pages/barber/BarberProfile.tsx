import React from 'react';
import Profile from '../Profile';

/**
 * Barber Profile Page
 * Uses the shared Profile component with barber-specific context
 */
const BarberProfile: React.FC = () => {
    return <Profile />;
};

export default BarberProfile;
